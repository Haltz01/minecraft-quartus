package io.github.marcuscastelo.quartus.item.tags;

import net.minecraft.nbt.CompoundTag;
import net.minecraft.tag.Tag;
import net.minecraft.util.Identifier;

import java.util.Collection;

public class CircuitTag extends Tag<String> {
    public CircuitTag(Identifier id) {
        super(id);
    }
}
